/**
 * DOM IDs used by the lint popover system.
 *
 * Includes the popover container, anchor element, and content card.
 */
export const ID = {
	lintPopover: 'lintPopover',
	lintPopoverAnchor: 'lintPopoverAnchor',
	lintPopoverCard: 'lintPopoverCard',
} as const;
